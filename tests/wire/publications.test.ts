// This file was auto-generated by Fern from our API Definition.

import * as Beehiiv from "../../src/api/index";
import { BeehiivClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PublicationsClient", () => {
    test("index (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                {
                    id: "pub_ad76629e-4a39-43ad-8055-0ee89dc6db15",
                    name: "Bee Informed",
                    organization_name: "Barry's Hiiv",
                    referral_program_enabled: true,
                    created: 1715698529,
                    stats: {
                        active_subscriptions: 12,
                        active_premium_subscriptions: 2,
                        active_free_subscriptions: 10,
                        average_open_rate: 0.8,
                        average_click_rate: 0.45,
                        total_sent: 12,
                        total_unique_opened: 9,
                        total_clicked: 6,
                    },
                },
            ],
            page: 1,
            limit: 10,
            total_results: 1,
            total_pages: 1,
        };
        server.mockEndpoint().get("/publications").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.publications.index();
        expect(response).toEqual({
            data: [
                {
                    id: "pub_ad76629e-4a39-43ad-8055-0ee89dc6db15",
                    name: "Bee Informed",
                    organization_name: "Barry's Hiiv",
                    referral_program_enabled: true,
                    created: 1715698529,
                    stats: {
                        active_subscriptions: 12,
                        active_premium_subscriptions: 2,
                        active_free_subscriptions: 10,
                        average_open_rate: 0.8,
                        average_click_rate: 0.45,
                        total_sent: 12,
                        total_unique_opened: 9,
                        total_clicked: 6,
                    },
                },
            ],
            page: 1,
            limit: 10,
            total_results: 1,
            total_pages: 1,
        });
    });

    test("index (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server.mockEndpoint().get("/publications").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.publications.index();
        }).rejects.toThrow(Beehiiv.BadRequestError);
    });

    test("index (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server.mockEndpoint().get("/publications").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.publications.index();
        }).rejects.toThrow(Beehiiv.NotFoundError);
    });

    test("index (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server.mockEndpoint().get("/publications").respondWith().statusCode(429).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.publications.index();
        }).rejects.toThrow(Beehiiv.TooManyRequestsError);
    });

    test("index (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server.mockEndpoint().get("/publications").respondWith().statusCode(500).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.publications.index();
        }).rejects.toThrow(Beehiiv.InternalServerError);
    });

    test("show (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: {
                id: "pub_ad76629e-4a39-43ad-8055-0ee89dc6db15",
                name: "Bee Informed",
                organization_name: "Barry's Hiiv",
                referral_program_enabled: true,
                created: 1715698529,
                stats: {
                    active_subscriptions: 12,
                    active_premium_subscriptions: 2,
                    active_free_subscriptions: 10,
                    average_open_rate: 0.8,
                    average_click_rate: 0.45,
                    total_sent: 12,
                    total_unique_opened: 9,
                    total_clicked: 6,
                },
            },
        };
        server
            .mockEndpoint()
            .get("/publications/pub_ad76629e-4a39-43ad-8055-0ee89dc6db15")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.publications.show("pub_ad76629e-4a39-43ad-8055-0ee89dc6db15");
        expect(response).toEqual({
            data: {
                id: "pub_ad76629e-4a39-43ad-8055-0ee89dc6db15",
                name: "Bee Informed",
                organization_name: "Barry's Hiiv",
                referral_program_enabled: true,
                created: 1715698529,
                stats: {
                    active_subscriptions: 12,
                    active_premium_subscriptions: 2,
                    active_free_subscriptions: 10,
                    average_open_rate: 0.8,
                    average_click_rate: 0.45,
                    total_sent: 12,
                    total_unique_opened: 9,
                    total_clicked: 6,
                },
            },
        });
    });

    test("show (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.publications.show("publicationId");
        }).rejects.toThrow(Beehiiv.BadRequestError);
    });

    test("show (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.publications.show("publicationId");
        }).rejects.toThrow(Beehiiv.NotFoundError);
    });

    test("show (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId")
            .respondWith()
            .statusCode(429)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.publications.show("publicationId");
        }).rejects.toThrow(Beehiiv.TooManyRequestsError);
    });

    test("show (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.publications.show("publicationId");
        }).rejects.toThrow(Beehiiv.InternalServerError);
    });
});
