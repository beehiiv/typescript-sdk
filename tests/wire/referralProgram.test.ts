// This file was auto-generated by Fern from our API Definition.

import * as Beehiiv from "../../src/api/index";
import { BeehiivClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ReferralProgramClient", () => {
    test("show (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                {
                    id: "mile_00000000-0000-0000-0000-000000000000",
                    auto_fulfill: true,
                    num_referrals: 1,
                    reward: {
                        id: "rew_00000000-0000-0000-0000-000000000000",
                        name: "name",
                        description: "description",
                        image_url: "image_url",
                        type: "physical",
                    },
                },
            ],
            limit: 1,
            page: 1,
            total_results: 1,
            total_pages: 1,
        };
        server
            .mockEndpoint()
            .get("/publications/pub_00000000-0000-0000-0000-000000000000/referral_program")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.referralProgram.show("pub_00000000-0000-0000-0000-000000000000");
        expect(response).toEqual({
            data: [
                {
                    id: "mile_00000000-0000-0000-0000-000000000000",
                    auto_fulfill: true,
                    num_referrals: 1,
                    reward: {
                        id: "rew_00000000-0000-0000-0000-000000000000",
                        name: "name",
                        description: "description",
                        image_url: "image_url",
                        type: "physical",
                    },
                },
            ],
            limit: 1,
            page: 1,
            total_results: 1,
            total_pages: 1,
        });
    });

    test("show (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId/referral_program")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.referralProgram.show("publicationId");
        }).rejects.toThrow(Beehiiv.BadRequestError);
    });

    test("show (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId/referral_program")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.referralProgram.show("publicationId");
        }).rejects.toThrow(Beehiiv.NotFoundError);
    });

    test("show (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId/referral_program")
            .respondWith()
            .statusCode(429)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.referralProgram.show("publicationId");
        }).rejects.toThrow(Beehiiv.TooManyRequestsError);
    });

    test("show (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new BeehiivClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            status: 1,
            statusText: "statusText",
            errors: [
                { message: "message", code: "code" },
                { message: "message", code: "code" },
            ],
        };
        server
            .mockEndpoint()
            .get("/publications/publicationId/referral_program")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.referralProgram.show("publicationId");
        }).rejects.toThrow(Beehiiv.InternalServerError);
    });
});
